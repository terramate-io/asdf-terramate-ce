# Contributing

Testing Locally:

```shell
asdf plugin test terramate https://github.com/terramate-io/asdf-terramate-ce.git "terramate version"
```

Tests are automatically run in GitHub Actions on push and PR.
